# SignPath Windows 签名接入（暂缓）

> **当前不申请、不启用。** 仓库许可已改为个人免费 / 商业收费（非 OSI），无法使用 SignPath Foundation 免费 OSS 签名。  
> 下文仅作将来参考；日常发版见 [`release.md`](./release.md)。未配置 `SIGNPATH_*` 时 CI 自动发未签名包。

若以后需要签名：自购 OV 证书，或将许可改回 OSI 开源后再走 SignPath。

---

## （归档）原 OSS 申请步骤

1. https://signpath.io/product/open-source  
2. 仓库须为 OSI 许可 + 公开仓 + 免费下载  
3. Project / policy / artifact 配置见历史版本与根目录 `SIGNPATH.md`  
4. GitHub Secrets：`SIGNPATH_API_TOKEN`、`SIGNPATH_ORGANIZATION_ID`
